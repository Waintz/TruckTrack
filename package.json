{
  "name": "trucktrack-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "next": "^15.4.2",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "turbo": "^1.13.4"
  },
  "scripts": {
    "postinstall": "npm rebuild lightningcss --update-binary",
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "dependencies": {
    "tailwind": "^4.0.0"
  }
}
